/**
 * case model
 */

export interface uploaderSettings {
  theme?: String,
  tip?: String,
  fileSize?: Object,
  fileCount?: Number,
  isImageType?: Boolean,
  isMultiple?: Boolean
  isAuto?: Boolean,
}